{
  "name": "docker-manager",
  "version": "1.0.0",
  "description": "Sistema web-based per gestire container Docker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "docker",
    "container",
    "web",
    "management",
    "api"
  ],
  "author": "Tu",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dockerode": "^4.0.2",
    "express": "^4.18.2",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
